React-Nextjs session 23-03-2026
Git Url:https://github.com/aniljos/react-nextjs-march-2026/
Git Url: https://github.com/aniljos/Nextjs-Feb-24-2025
REST API Mock: https://github.com/aniljos/REST-API-Mock

React CDN Links: https://legacy.reactjs.org/docs/cdn-links.html
Babel cdn: https://cdnjs.com/libraries/babel-standalone/7.26.3

Introduction: https://forms.gle/JJUREb7AcNcX5TXv5
Quiz #1: https://forms.gle/qjXX2bLjauSy7D5LA
Quiz #2: https://forms.gle/TTbaJ5xTtf5LfM8w9
Quiz #3: https://forms.gle/nqXeDHsZ8qLXoRUH7

-deploymnet
-dev run     
 ->npm run dev 

-prod run
  -> npm run build
  -> npm start 

React Components

Types of components(based on coding): Functional and Class
Types of components( based on rendering): client-side rendering(CSR) and server-side rendering(SSR)

In Nextjs: default rendering is SSR

-React Component props
	- parameterize a component
	- passing data from parent to child and vice-versa
	- changes to props will re-render the component
	- props is read-only
React Component state
	- internal to the component
	- changes to state will re-render the component
	- state changes are asynchronous
	- state changes are batches
	- setState
		- setState(updatedValue)
		- setState(callback)   (updatedValue callback(prevValue))
React effects(useEffect)
	- Hook into the lifecycle
	- mount: 
		- called once the component is mounted
		- dependency array is empty
		- API calls 
		- DOM initialization
		- Register to any events(document/window)
	-unmount
		- called once when the component is unmounted
		- cancel the api calls
		- Unregister to any events(document/window)
	- update
		- called when the component is rerendered
		


input element
	- binding using state(useState) => value, onChange : Controlled input
	- Reference(useRef) to the input and read the value : Uncontrolled input

Navigation in React	
	- There no apis that React provides for Navigation
	- react-router-dom: 
		- library for navigation
		- Create-React-App or Vite 
	- Next.js has a nvagiation library(The App and Page router)
				
React Design & Optimizations
	- Components are granular( small components)
	- Custom Hooks: reuseable, function thats uses existing hooks.
	- memoization: 
		- like caching 
		- React.memo(component)
		- The component re-renders only if the props or state changes

Session State(in the browser)
	- LocalStorage & SessionStorage
	- Cookies
	- Memory
	
Redux
	- Action is an object with a type & a payload
		- {type: "save_token", token: "abc"}
		- {type: "clear_token"}
	- Reducer is a function 
		- receives the actions
		- updates the store
React-redux
	- Provider: component to integrate the store with the react application
	- Hooks	
		- useDispatch: returns the dispatch method
		- useStore: returns a reference to the store
		- useSelector: return a subset(of the state) in redux and subscribe to the store
Redux Tooklit
	- implementations of redux patterns
	- opiniontaed framework
	-slices => alernative to reducers
	- treat state as mutable
	- genarates reducer code
	-genarates the action creater (actions creators )

-State Management libraries
  react context
  mobs


-client component
   rerendered and hydrated(invoke effect, state..etc and html genarates.) in the browser
   can be used React hooks like useState,useEffect,redux..etc
-server component
   rendered on the server with no client-side java script and available with the app router!
   available from the nexjs 13 with app router
   can not use client side hooks like useState, useEffect, redux,react context.. etc onclick..
   integrate-> server component work along with the client component, providing 
   seemless developer experience within the same react tree
   server component ideal for fetching data from databases or APIs without exposing sensitive logic to the client.
   mainly for static content!

-static page(SSG - Static Site Generation)
 pre-rendered as static content
 will be prepared and keep ready at the time of build its self and catched,
 it will rerender again after app run will be used from cache only
 How to make static to SSG(ssr) ?
   //making force full dynamic - option 1
    const contentTypeHeader = (await headers()).get("Content-Type")
   //making force full dynamic - option 2
    export const dynamic = 'force-dynamic';


-dynamic page(SSR- Server Side Rendering)
  rendered as content on demand or on request!
  as componet has props and params then contnet will be dynamic loaded.

-incrementail staic genarate(ISG)
 // This tells Next.js to regenerate this page at most every 60 seconds
 export const revalidate = 60;


-Next js is a fullstack framework.
	there are several ways to create and exposr backend APIS,
	API routes the traditional way we use is with page router 

	rout handlers:
	 route handlers are used to define ai endpoints in next js when using app router

   file based routing
    api routes are created by defining route.ts or route.js	 

-next js Features
 -full-stack framework
 -file-based routing
 -clientside rendering
 -server side rendering
 -static site genaratation SSG
 -incremental static regeneratation ISR
 -hybrid rendering
 -api routes
 -automatic code spliting 
 -built in css saas support

 -fragment <></>
  -in jsx should have at least one parent element so instead div we use fragemnt <> </> 
   so it will not accupy any space in dom


-MFE micro frontends approaches
	Webpack Module Federation
	Single APA 
	Piral
	Luigi

-vite does not use webpack it uses rollerconfig
-next js uses Turbopack 






 	
	


